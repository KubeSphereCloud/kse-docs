---
title: "服务器 BIOS 配置规划"
weight: 4
---

不同机型的服务器，其 BIOS 配置不同，需根据实际情况进行配置，以下仅供参考：

* 系统启动引导模式设置为 BIOS。
* 系统时间为 UTC+8（建议设置时间为当前北京时间 -8 小时，每台云易捷服务器的时间误差 < 3 秒）。
* 开启 CPU 虚拟化设置。
* 开启 CPU 超线程。
* 关闭 NUMA 设置。
* 选择最大性能（Maximum Performance）。
* IPMI 配置。

