// :ks_include_id: 41d81fc212244bc2a84f09af45a087c4

* In single-node mode, the node serves as the control node and the worker node.
* In multi-node mode, the node role is specified when you install {ks_product} or add a node. A node can run as a control node or a worker node, or a control and worker node at the same time.
* You can create VMs on worker nodes.