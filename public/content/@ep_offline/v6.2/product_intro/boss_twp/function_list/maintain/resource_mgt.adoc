---
title: "资源管理"
weight: 2

---




[cols="1,1,3a"]
|===
|类别|功能特性  |描述

.4+^.^|计算 | 云服务器| 
* 多维度、分区域列表展示{product_name}中已创建的云服务器信息，分区域展示云服务器的统计信息，可供管理员查看、检索以及导出云服务器列表。
* 支持批量管理云服务器，如批量启动、关闭、删除云服务器，批量迁移云服务器，批量转移租户等。
* 展示云服务器关联资源、租赁信息、操作日志，以及内存使用率、系统盘 IOPS 等监控数据。
* 提供云服务器远程终端的入口，提供 CLI 的入口。
* 支持调整云服务器的限速。

| 镜像 a| 
* 分区域展示镜像的统计数据，分区域列表展示系统镜像与自有镜像，可供管理员查看、检索目标镜像。
* 支持镜像的上架、下架、修改、删除以及导出操作。
* 支持注册镜像以及跨区域复制镜像。

| SSH 密钥 a| 
* 分区域列表展示{product_name}中已创建的 SSH 密钥，可供管理员查看、检索目标镜像。
* 支持查看 SSH 密钥的公钥，以及关联的云服务器列表。

| 安置策略组 a| 
* 分区域列表展示{product_name}中已创建的安置策略组，可供管理员查看、检索目标镜像。
* 针对具体安置策略组，列表展示绑定该策略组的云服务器。


.7+^.^|高性能计算 | 逻辑集群| 

| 作业管理| 
| 软件中心| 
| 图形服务| 
| 操作日志|
| 审批管理|  
| 用量统计|

.7+^.^|网络 | VPC 网络| 

| 私有网络| 
| 基础网络| 
| 公网 IP| 
| 负载均衡器|
| 边界路由器|  
| 网络流量镜像|



|===





// [cols="1,1,3a"]
// |===
// |类别|功能特性  |描述

// .4+^.^|计算 | 批量操作| * 支持批量将云服务器从一个云租户迁移至另外的云租户。
// * 支持批量迁移云服务器。
// * 支持批量启动、关闭、删除云服务器。

// | 云服务器列表 a| * 分区域展示{product_name}的云服务器统计信息。
// * 多维度列表展示云服务器信息。包括基本信息与监控信息，如内存使用率、系统盘 IOPS 等数据，可供管理员查看与检索。
// * 支持导出云服务器列表。

// | 云服务器详情 a| * 展示云服务器关联的资源、租赁信息以及操作日志。
// * 

// | 云用户管理 a| * 支持创建、删除云用户，支持批量修改云用户配额。
// * 支持云用户基本信息修改、角色权限修改、付费类型修改、密码重置、修改登录邮箱等操作。
// * 支持禁用、启用、删除云用户。
// * 支持云用户的资源回收、资源转移等操作。

// | 云用户认证 a| * 支持对云用户进行个人认证或企业认证。
// * 展示当前{product_name}中用户认证的统计数据，列表显示云用户认证记录。

// .2+^.^|网络 | 第三方用户列表 a|* 列表展示{product_name}上注册的所有第三方用户，可供管理员查看与检索。
// * 列表信息包括第三方用户的状态、第三方账号、用户邮箱及 ID、登录服务器、登录的服务器类型以及用户创建的时间。


// | 云用户绑定 a|* 支持与云用户的绑定与解绑操作。
// * 支持查看绑定的云用户详情以及对绑定的云用户进行管理操作。



// .2+^.^|存储 | 创建控制台登录服务器 | 创建控制台登录服务器，与用户已有的服务器进行对接，使得用户可以通过已有账号登录管理控制台。

// * 支持服务器类型有 AD 服务器与 LDAP 服务器。

// | 登录服务器列表 a|* 列表展示{product_name}上已创建的所有登录服务器，可供管理员查看与检索。
// * 列表信息包括登录服务器的状态、服务器地址、服务器端口、是否使用 SSL 以及服务器创建的时间。


// |安全  | API 密钥列表| * 列表展示{product_name}上已创建的所有 API 密钥，可供管理员查看与检索。
// * 列表信息包括 API 密钥的状态、名称、创建 API 的云用户邮箱及 ID、设置的白名单以及创建时间。
// * 与创建该密钥的云用户关联，可查看相应的云用户信息。

// .2+^.^|AppCenter 应用  | 配额详情|按区域与产品类型，展示默认配额。

// | 配额管理| 支持管理员按需求、区域以及产品类型，调整资源配额。


// |===




// 云环境中的每项 IT 资源，BOSS 平台都以信息列表的形式来描述其具体配置、状态、资源所有者等信息，并且支持模糊搜索，以方便管理员在资源众多的情况下迅速定位资源。资源列表页面中的一个条目往往包含了不同资源之间的关联信息，管理员可以直接点击相关联资源进入关联资源的详情页面查看其信息，也可点击资源所有者查看用户信息。

// 对于关键的云资源，平台支持监控模式的资源信息列表，以条形比例图和环形比例图直观、准确地表现出资源占用率，使用压力等信息，更适合运维人员在工作闲时状态下的浏览与监控。

// 管理员可以自定义资源列表列的展示顺序与是否显示，提升列表展示的灵活性。同时，对列表页的报表导出功能旨在为使用者对云资源的使用情况做数据分析提供可靠支持。

// 当鼠标悬浮于某一可操作的资源信息行时，会出现快捷操作的菜单，以支持管理员快速完成作业。

// 每种资源列表的顶部，会有对该种资源的汇总统计数据，管理员可以依赖该汇总对 IT 资源做出自己的容量规划。

// == 计算

// //这不是功能介绍，需要修改

// 青云的计算虚拟化基于 KVM（Kernel-based Virtual Machine）技术，KVM 是基于 Linux 内核的虚拟化技术，自 Linux 2.6.20 之后集成在 Linux 的各个主要发行版本中。KVM 目前已成为学术界的主流 VM 之一。KVM 虚拟化得到硬件厂商的支持（如 Intel VT 技术或者 AMD V 技术），是基于硬件的完全虚拟化，因此性能一举超过其他的虚拟化技术，经过青云调优之后的 KVM 更是将这种性能发挥到极致。通过 KVM 虚拟化技术，实现对服务器物理资源的抽象，将 CPU、内存、I/O 等服务器物理资源转化为一组可统一管理、调度和分配的逻辑资源，并基于这些逻辑资源在单个物理服务器上构建多个同时运行、相互隔离的虚拟主机执行环境，实现更高的资源利用率，同时满足应用更加灵活的资源动态分配需求。利用硬件辅助虚拟化技术，具有更高的性能、可用性和安全性特点，结合上层云操作系统管理功能，可实现更低的运营成本、更高的自动化管理水平和更快速的业务响应速度。对用户呈现标准的虚拟主机，这些虚拟机就像同一个厂家生产的系列化的产品一样，具备系列化的硬件配置，使用相同的驱动程序。为了满足不同类型的计算场景，青云还提供了基于 LXC（Linux Container）技术的容器主机。


// == 网络

// 传统数据中心三层网络架构，受到 STP 协议限制，只能通过"树"状节构进行网络数据传输。树状网络结构的规模越大，节点之间转发路径越长，相互间数据传输延迟随之增加。并且，在三层网络核心节点上，也存在引发全网数据传输中断的风险。同时在传统网络中还存在 VLAN 划分数量限制、同网段主机数量不能过大等问题。导致传统网络企业用户难以向云计算转型。

// SDN 以软件方式完整再现了物理网络。虚拟网络不仅可以提供与物理网络相同的功能特性和性能保证，而且还具有虚拟化的运维优势和硬件独立性，包括快速调配、 无中断部署、自动维护等。网络虚拟化将逻辑网络连接设备和服务（逻辑端口、交换机、路由器、防火墙、负载平衡器和 VPN 等）提供给已连接的工作负载。应用在虚拟网络上的运行与在物理网络上完全相同。使用 SDN 技术，实现网络控制平面和转发平面的分离，由此提供更友善、更强大的网络配置和控制能力。

// 青云通过网络软件定义（SDN）技术实现虚拟网络的编程控制和网络功能虚拟化（NFV）。青云企业云平台为承载的虚拟机提供了两种网络：基础网络和私有网络。为便于说明，以下图为例进行介绍。

// //为便于说明，以下图为例(这是私有云部署情形。去掉企业网络部分，就是青云公有云情形)：

// //这个图太模糊，需要重新画

// image::/images/ep_offline/boss_twp/30_10.png[]

// 下表是关于两种网络的对比说明。


// [cols="1,2,2"]
// |===
// |对比项|基础网络|VPC 网络

// |与企业现有网络的关系和ip段分配
// |对于企业云环境，基础网络可以理解为一个或几个 VLAN 段，其网段需要符合企业对其内部网络的规划。
// |VP 网络是一个独立的网络，其内部往往配置成一个 16 位掩码的私有网段。该网段与企业现有网络没有任何关系。

// 常见可用的 VPC 网段有 `192.168.0.0/16` 以及 `172.16.0.0/16 ~ 172.31.0.0/16`。

// |与企业现有网络的隔离性
// |基础网络一般与企业现有网络默认三层可达。 +
// 创建在基础网络的虚拟机，与企业网络现有服务器，在防火墙允许的情况下默认可以互相 ping 通。 +
// 以上图为例，host0 可以直接访问 VM1、VM2。
// |VP 网络一般与企业现有网络默认不可达。
// 创建在 VPC 网络的虚拟机，与企业网络现有服务器，不配置隧道、端口映射的情况下默认无法互相 ping 通。
// 以上图为例，host0 默认无法访问 VM5、VM6、VM7、VM8。

// |企业网内用户访问网络上虚拟机的方式
// |对于企业云用户，在企业网络上一般可以直接访问基础网络的服务器。如上图，host0 可以与 VM1 互访。
// |如上图。对于企业云用户，在管理路由器3上设置了端口映射以后，host0 可以访问 VPC 网络a 内服务器相应端口提供的服务。Host0 也可以通过与管理路由器3建立隧道，使得 host0 也具有 VPC 网络a 的 IP 段地址，进而可以直接访问 VPC 网络a内的所有虚拟机。

// |用户自定义
// |云平台租户没有基础网络的配置权限，只能直接使用基础网络提供的网络服务。 +
// 云平台运维人员（管理员）可以按需创建基础网络。
// |云平台租户可以在获得配额后按需创建、删除、配置 VPC 网络

// |私有网络
// |没有私有网络的概念。
// |私有网络是 VPC 网络的一个子网，一般配置为一个 24 位网段。由于私有网络的存在，用户可以方便地将整个私有网络从一个 VPC 转移到另一个 VPC，可以为私有网络配置独立的路由表。
// 私有网络是青云平台网络的最小实体，每个私有网络具有独立的 vni，不同私有网络之间二层隔离。

// |===


// 通过上面的对比可以了解到以下的特点：

// * 基础网络 - 云平台的基础服务。简单方便，配置少。
// * VPC网络 - 云平台的可选服务，基于基础网络。灵活，隔离性好，但是需要更多的配置。

// 青云对基础网络和 VPC 的私有网络进行了广播包抑制。广播包是网络通信的必备组成要素，在网络连接建立的时候，会从源端口向网络发出广播包，询问目的端口所在的位置，广播包被正确响应后，数据连接才可以建立，并进行数据传输。但网络中如果有过多网络端口出现，势必会发出过多广播。过多的广播包不仅占用网络带宽资源，还有可能引发网络故障，如引发 ARP 风暴的原因之一就是网络内过多广播包存在，影响了网络正的数据通讯的传输。VXLAN 所采用的是一种隧道封装技术，连接建立时，形成的是物理点对点连接的隧道，不会有杂乱的广播包在物理网络接收进来或传播出去。同时当虚拟主机端口向错误地址发出连接请求时，广播包首先会向网关（分布式 SDN 控制器）询问，当请求地址不在控制器管理列表内时，广播包就会被丢弃。不会在基础网络内进行广播，起到了广播消减的作用。

// == 存储

// 青云云平台为云服务器提供了块级存储设备，支持多种规格和类型，包括性能型硬盘、超高性能型硬盘和容量型硬盘，并且支持对云服务器和块存储进行备份。

// 青云的块存储设备具备优异的性能，并且通过软件定义存储 SDS2.0 和分布式块存储 NeonSAN 的功能，让您可以灵活的选择副本策略，任何时候不用担心数据丢失的问题。

// 业务正常运行时也可以进行在线备份，并可随时回滚，满足用户各方面的使用需求。

// 云计算管理平台管理云平台上各类存储数据。

// == 安全

// 青云QingCloud 平台十分重视账号的权限与用户的安全，账号代表着用户在云平台上的唯一凭据，在青云平台上创建一个账号，对我们来说就是代表着用户对我们的信任。

// 用户在青云上的资源是重要的 IT 资产，因此青云非常重视用户资源、网络、账户的安全性，用户可以通过以下功能提高自己账户以及资源的安全性：

// * 安全组：通过策略配置，安全组能极大地提高一个内部网络或云服务器的安全性，并过滤不安全的服务，从而降低内部网络或云服务器的风险。
// * Web 应用防火墙：Web 应用防火墙通过检查 HTTP/HTTPS 流量来实现内容过滤， 可以防止针对 Web 应用程序漏洞的攻击，如 SQL 注入、跨站点脚本（XSS）、文件包含漏洞以及安全配置错误等。 CC 防护功能可以降低攻击对系统的影响，通过自定义规则可以灵活有效的阻止非法流量，保证应用的稳定运行。WAF 部署在负载均衡器（LoadBalancer）之上。
// * 云防火墙：云防火墙用于防护进出 EIP 的网络攻击。用户开通云防火墙后 EIP的流量将被自动引流至云防火墙。经过云防火墙的网络访问会经过拦截策略、入侵防护策略的检查，有威胁的访问会被拦截，并产生安全事件日志。用户可以自定义拦截规则，选择入侵防护的规则，可以看作是安全组功能的加强版。
// * 网络访问控制：网络 ACL 与防火墙的功能类似，但是网络 ACL 绑定给私有网络，防火墙一般直接绑定给云服务器或者负载均衡器。网络 ACL 中更适合配置私有网络 Vxnet 通用的安全规则。
// * 秘钥管理服务：可以帮助用户轻松创建和管理密钥，保护密钥的保密性、完整性和可用性，满足多应用多业务的密钥管理需求，并符合监管和合规要求。

// == Appcenter

// 云计算管理平台中的 AppCenter 包括 AppCenter 集群、管理集群、云应用、SaaS 应用、License 应用、应用审核和开发者认证。在云计算管理平台可对以上应用以及集群进行管理与分配。

// AppCenter 包括开发者控制台和用户控制台及调度系统三大部分，最终用户在青云官网上可以浏览服务商提供的 App 并创建 App 实例。个人用户和企业用户可以在开发者控制台开发供自己内部使用的 App。开发应用的时候，除了填写必要的应用信息外，还要上传一个配置包，这个配置包含 config.json，cluster.json.mustache 和国际化语言包，这些文件是一种描述性的集群实例信息的文件。使用者在创建该 App 实例的时候，青云的调度系统会根据这个配置包定义的信息创建基础资源，然后注册集群信息到青云为每个 VPC 环境下部署的 metadata service 中，集群主机通过 confd（配合 toml，tmpl 模版配置文件系统）进程检查 metadata service 的变化，来刷新应用配置文件，然后启动应用服务。

// == 控制台管理

// 云计算管理平台中的控制台管理包括定时器、监控告警、云服务器启动配置以及自动伸缩策略，负责对云平台的进行任务发放、监控和资源启用等。
