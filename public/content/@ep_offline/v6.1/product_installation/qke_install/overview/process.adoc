---
title: 部署流程
weight: 1
---

{product_name}上安装部署 QKE 的流程分为后端操作与前端配置两个部分。两者缺一不可。

* 后端操作主要是在{product_name}上通过脚本，安装 QKE 的软件包，并在相应的数据库中添加 QKE 的初始数据。
* 前端配置是使得用户可以通过 Console 成功创建目标的 QKE 集群。

详细操作可参考后文。

// 在{product_name} v6.1 版本安装 QKE 流程如下：

// . link:../../prepare/resources[下载资源包]
// . link:../../prepare/import/upload_docker_image[导入资源包]
// . link:../../install[安装并配置 QKE]
