---
title: 支持一云多芯场景
weight: 03
---

当开启 PaaS 专用安置组，根据应用和主机类型可以匹配到唯一安置组，如果未开启 PaaS 专用安置组，根据主机类型匹配到唯一安置组。

平台根据**主机类型**可以匹配到唯一安置组（如果配置了 PaaS 专用安置组，将根据**应用**和**主机类型**匹配到唯一安置组）。

但在信创环境中，X86 和 ARM 架构在相同的主机类型下会有不同的安置组，这种情况下 AppCenter 会默认指定一个安置组，确保集群安置成功。
