---
title: 获取存储的配置信息
weight: 10
---

需要先获取如下配置信息：

* 存储 IP 地址
+
若用户侧已完成存储部署，请联系用户侧获取存储 IP 地址。

* 存储 iqn 标识符
 ** 可以在存储服务器上执行 `targetcli` 命令进入 CLI，执行 `ls` 命令进行查看。
 ** 如果存储有 Web 可视化页面，也可以直接在界面上进行查看，具体可以查看存储产品的文档。