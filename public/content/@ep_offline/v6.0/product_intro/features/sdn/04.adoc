---
title: VLAN VBC 支持 VIP
weight: 04
---

VLAN 基础网络支持使用接口申请 VIP（虚拟 IP 地址）。

用户通过申请虚拟 IP 接口可以获得一个固定的 IP 地址，对于需要规划 IP 地址的用户，可以按照规划申请 IP 地址。

对于一些上层服务（例如 QKE）需要使用固定的 IP 地址作为服务节点地址，通过申请 VIP，可以获得固定的 IP 地址，不会因为业务重启而导致 IP 地址变化（默认的 DHCP 模式）。
