---
title: "补丁请求"
weight:  05
---

本节介绍如何为满足条件的 HTTP 请求添加额外的查询参数。

故障注入期间，通过 GET 方法访问注入 Pod 时，请求路径将自动附加查询参数 `a=b`。

== 操作步骤
. 登录 KubeSphere Web 控制台，进入目标集群，在左侧导航栏点击 *混沌工程*。
. 点击 *创建*，选择故障类型为 *HTTPChaos*，配置以下参数后点击 *创建*。

[%header,cols="1a,4a"]
|===
|参数 |描述

|目标（target）
|选择 `Request`，对请求生效。

|端口（port）
|监听端口，例如 `80`。

|请求方法（method）
|触发故障的 HTTP 方法，例如 `GET`。

|追加参数（patch.queries）
|以键值对形式追加的查询参数，例如键 `a`、值 `b`。

|持续时间（duration）
|故障持续时间，例如 `10m`。

|生效范围（selector）
|选择故障注入的目标命名空间和 Pod。
|===


验证故障效果：查看 Pod 日志，正常情况下记录 `"GET / HTTP/1.1" 200`；故障注入期间记录 `"GET /?a=b HTTP/1.1" 200`。
