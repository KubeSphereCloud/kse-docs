---
title: "查看集群网关实现的信息"
weight:  02
---

本节介绍如何查看集群网关实现的信息。

## 前提条件

- KubeSphere 平台需要安装并启用 **Gateway API 网关**扩展组件。

- 您需要加入一个集群并在集群中具有 **cluster-admin** 权限。

## 操作步骤

1. 以具有 **cluster-admin** 权限的用户登录 KubeSphere Web 控制台并进入您的集群。

2. 在左侧导航栏选择 **Gateway API 网关 > 网关实现**。

3. 在**集群网关实现**页签，查看集群网关实现的信息。

[%header,cols="1,4"]
|===
| 参数 | 描述

| 状态
| 网关实现的状态：正常、处理中、异常。

| 访问模式
a|
网关实现服务的外部访问模式：

* *NodePort*：系统将节点的端口映射到服务端口，可通过节点的 IP 地址和节点端口访问服务。
* *LoadBalancer*：系统在 NodePort 服务的基础上分配一个外部 IP 地址，并绑定外部负载均衡器。
+
外部负载均衡器监听服务对应的节点端口，从而可以通过外部 IP 地址访问服务。
+
该模式依赖负载均衡器插件和基础设施环境支持，并需要配置相关注解。

| 节点端口
| 网关实现服务的 TCP 和 TCP/TLS 节点端口，仅在访问模式为 NodePort 时显示。

| 网关
| 绑定到该网关实现的网关数量。

| 创建时间
| 网关实现的启用时间。

|===