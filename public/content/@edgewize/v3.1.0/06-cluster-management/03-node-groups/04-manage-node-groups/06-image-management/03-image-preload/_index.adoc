---
title: "镜像预热"
weight:  03
---

本节介绍如何为边缘节点管理“镜像预热”任务。

运行镜像预热任务，将所需的镜像提前加载到目标节点上，从而减少容器启动时间，提高系统的响应速度和稳定性。

EdgeWize 支持以下类型的镜像预热：

* 即时预热：用于管理仅运行一次的镜像预热任务。

* 定时预热：用于管理周期性运行的镜像预热任务。系统根据定时任务中设置的周期创建普通任务并运行。