* 网络设置 > 端口映射
+
--
配置端口映射后，满足主机端口的流量会映射到对应的容器端口。选择后，需配置容器端口和主机端口。

* 点击**添加端口**可设置多条端口映射规则。

* 在已创建的映射规则右侧点击image:/images/ks-qkcp/zh/icons/trash-light.svg[trash-light,18,18]可删除规则。
--

* 网络设置 > 主机网络
+
使用主机（边缘节点）的网络，即容器与主机不存在隔离层，使用同一个IP。