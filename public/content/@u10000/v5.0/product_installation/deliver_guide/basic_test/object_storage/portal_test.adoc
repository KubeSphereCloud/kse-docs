---
title: Portal 功能测试
weight: 3
---


== 测试目的

{product_name} 的 Portal 能正常使用。



== 预置条件

{product_name} 存储系统已经部署完毕，且运行正常。


== 测试过程

. 登录 Portal。

. 点击访问 Portal 各个页面。

. 查看**总览**页面物理资源显示。

. 查看**总览**页面集群容量显示。

. 点击**对象存储 > Bucket**，查看 Bucket 列表。

. 点击 Bucket 名称，进入 Bucket 详情页，确认各项信息是否显示正确。

. 点击**对象存储 > 业务指标**，进入资源子页面，查看相关信息。

. 点击**首页 > 用户 > 菜单**，查看用户列表信息。

. 点击用户 ID，进入用户详情页面，查看相关信息。

. 点击 API 密钥菜单，查看 API Key 列表。

. 点击管理页面，查看管理员列表。


== 预期结果

. 步骤1，能成功登录 Portal。

. 步骤2，能正常访问 Portal 各个子页面。

. 步骤3，首页节点数显示正确。

. 步骤4，首页磁盘容量显示正确。

. 步骤5，首页 Bucket 数量显示正确。

. 步骤6，成功进入 Bucket 菜单，且 Bucket 列表显示正确。

. 步骤7，操作无报错，且 Bucket 各项信息显示正确。

. 步骤8，操作无报错，且资源子页面各项信息显示正确。

. 步骤9，操作无报错，且用户列表信息显示正确。

. 步骤10，操作无报错，且用户信息显示正确。

. 步骤11，操作无报错，且 API Key 信息显示正确。

. 步骤12，操作无报错，且管理员显示正确。




== 验证结果


根据实际情况进行填写。

== 备注

无。
