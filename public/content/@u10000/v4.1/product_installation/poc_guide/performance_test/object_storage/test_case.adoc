---
title: 测试用例
description: 
weight: 2
---


// == 多客户端单桶小文件性能


// [cols="1,3", options="header"]
// |===
// | 用例编号 | 01

// |测试目的
// |验证 {product_name} 对象存储小文件的性能表现。

// |预置条件
// a|
// . {product_name} 已经安装部署完毕，且运行正常，无故障、无报错、测试过程中确保无其他关联影响项。
// . COSBench 工具已安装配置完成。


// |测试步骤
// a|
// . 多客户端同时访问 1 个存储桶。
// . 30% 写，写文件建议大小：4KB-4MB；70% 读，读文件建议大小：4KB-4MB。
// . 记录不同并发下对象存储性能表现，重点关注 IOPS，并取最高值。

// |预期结果
// a|
// 测试过程中，无异常出现。

// |实测结果 |

// |===


// == 多客户端多桶大文件性能


// [cols="1,3", options="header"]
// |===
// | 用例编号 | 01

// |测试目的
// |验证 {product_name} 对象存储多客户端大文件性能表现。

// |预置条件
// a|
// . {product_name} 已经安装部署完毕，且运行正常，无故障、无报错、测试过程中确保无其他关联影响项。
// . 当前环境，COSBench 工具已安装配置完成。
// . 创建 1 个对象存储桶。


// |测试步骤
// a|
// . 多客户端同时访问 1 个存储桶。
// . 30% 写，写文件建议大小：4MB-64MB；70% 读，读文件建议大小：4MB-64MB。
// . 记录不同并发下对象存储性能表现，重点关注 Bandwidth，并取最高值。

// |预期结果
// a|
// 测试过程中，无异常出现。

// |实测结果 |

// |===

// == 高水位多客户端单桶文件性能


// [cols="1,3", options="header"]
// |===
// | 用例编号 | 01

// |测试目的
// |验证 {product_name} 对象存储高水位多客户端单桶文件性能。

// |预置条件
// a|
// . {product_name} 已经安装部署完毕，且运行正常，无故障、无报错、测试过程中确保无其他关联影响项。
// . 当前环境，COSBench 工具已安装配置完成。
// . 创建 1 个容量为 60TB 的对象存储桶。
// . 创建 5 个容量分别为 10TB 的对象存储桶。
// . 且预埋 60% 存储容量的数据。


// |测试步骤
// a|
// . 多客户端同时访问 1 个存储桶。
// . 30% 写，写文件建议大小为：4KB-64MB；70%读，读文件建议大小为：4KB-64MB。
// . 记录不同并发下对象存储性能表现，小文件重点关注 IOPS，大文件重点关注 Bandwidth，并取最高值。

// |预期结果
// a|
// 测试过程中，无异常出现。

// |实测结果 |

// |===

目前 COSBench 内置的测试用例可以覆盖多数情况。若用户无特定要求，仅需执行内置的测试用例即可。具体操作步骤可参考：link:../cosbench/#_执行测试步骤[内置测试用例执行]。

针对有特许需求的场景，内置测试用例不满足需求，此时，可参考link:../../../additional/cosbench/[ COSBench 测试用例]编写相应的测试用例文件。
