---
title: "创建 Redis Cluster 实例"
keywords: "Kubernetes, {ks_product}, RadonDB, RadonDB Redis Cluster, 创建 RadonDB Redis Cluster 实例"
description: "介绍如何创建 RadonDB Redis Cluster 实例。"
weight: 01
---

:ks_db_product: Redis Cluster

本节介绍如何在 DMP 数据库管理平台上创建 RadonDB {ks_db_product} 实例。


== 前提条件

* 在{ks_product_both}平台加入一个企业空间和一个项目。
* {ks_product_right}平台需要安装 RadonDB DMP 扩展组件。

== 操作步骤

. 以 platform-admin 角色登录{ks_product_left} Web 控制台并进入数据库管理平台。
. 在左侧导航栏选择 **{ks_db_product}**。
. 在页面右侧选择**新建 {ks_db_product} 实例**。
. 在**基本信息**页签，设置 RadonDB {ks_db_product} 实例的基本信息和位置，然后点击**下一步**。
+
--
[%header,cols="1a,4a"]
|===
|参数 |描述

|名称
|{ks_db_product} 实例的名称。名称只能包含小写字母、数字和连字符（-），必须以小写字母开头并以小写字母或数字结尾，最长 32 个字符。

|描述
|{ks_db_product} 实例的描述信息。描述可包含任意字符，最长 256 个字符。

|位置
|{ks_db_product} 实例所属的企业空间和项目。
|===
--

. 在**应用设置**页签，设置 {ks_db_product} 实例的参数。
+
--
[%header,cols="1a,4a"]
|===
|参数 |描述

|版本
|{ks_db_product} 应用的版本。

|数据库内核
|{ks_db_product} 数据库的内核版本。

|CPU/内存
|为每个节点预留的 CPU 和内存资源，同时也是每个节点可使用的 CPU 和内存资源上限。

|存储类 
|{ks_db_product} 数据库使用的存储系统对应的存储类。如果下拉列表中没有符合需要的存储类，请联系平台管理员创建存储类。

|卷
|每个节点使用的卷大小，单位为 GiB。

|分片数量
|{ks_db_product} 实例的代理节点数量。取值范围为 3 到 50。

|密码
|{ks_db_product} 数据库的访问密码。密码只能包含大写字母、小写字母、数字和特殊字符（!$%^&*().;），长度为 6 到 31 个字符。
|===

点击**高级设置**，启用或禁用**外部访问**、**反亲和性配置**和 **node 亲和设置**。

[%header,cols="1a,4a"]
|===
|设置
|描述

|外部访问
|启用外部访问后，可以使用域名从 KubeSphere 集群外部访问数据库分片。

|反亲和性配置
|开启反亲和性配置后， pod 将优先调度到不同的 node 上。

|node 亲和设置
|开启 node 亲和设置后，添加匹配 node 的键值对，可以将 pod 部署在特定条件的 node 上。

* 点击**添加**可以设置多个键值对。 
* 在已添加的键值对右侧点击image:/images/dmp/icons/trash-light.svg[trash,18,18]可删除键值对。
|===

include::../../../_ks_components/admonitions/tip.adoc[]

您还可以打开**应用设置**页签右上角的**编辑 YAML**，在 yaml 文件中自定义配置。

include::../../../_ks_components/admonitions/admonEnd.adoc[]

--

. 点击**确定**。RadonDB {ks_db_product} 实例创建后，将显示在 {ks_db_product} 页面的实例列表中。
