---
title: "创建数据库用户"
keywords: "Kubernetes, {ks_product}, RadonDB, Redis Sentinel, 管理告警规则组, 创建数据库用户"
description: "介绍如何创建数据库用户。"
weight: 01
---

:ks_db_product: Redis Sentinel

本节介绍如何创建数据库用户。


== 前提条件

* RadonDB {ks_db_product} 实例状态为**运行中**。


== 操作步骤

. 以 platform-admin 角色登录{ks_product_left} Web 控制台并进入数据库管理平台。

. 在左侧导航栏选择 **{ks_db_product}**。

. 在 {ks_db_product} 实例列表中，点击一个实例名称打开其详情页面。

. 在页面右侧点击**数据库用户**页签，然后点击**创建**。

. 在**创建用户**对话框，设置用户名和密码，然后点击**确定**。用户创建完成后将显示在用户列表中。
+
--
[%header,cols="1,5"]
|===
|参数 |说明

|用户名
a|自定义用户名称。

配置要求：用户名只能包含大写字母、小写字母、数字和下划线（_），必须以大写或小写字母开头，长度为 2 到 26 个字符。

|密码  
a|输入用户密码。

配置要求：密码必须包含至少一个大写字母、一个小写字母、一个数字和一个特殊字符（!@#$%^&*_+-=），长度为 8 到 32 个字符。

|权限
|根据 Redis 官方规则设置 ACL 以控制用户的权限。

|===

// Note
include::../../../../_ks_components/admonitions/note.adoc[]

* 不支持创建 **root** 或以 **radondb_** 开头的名称。

* 不支持创建同名用户。

include::../../../../_ks_components/admonitions/admonEnd.adoc[]
--

